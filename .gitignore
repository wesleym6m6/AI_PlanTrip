.envrc
trek/
trek-src/
.claude/
.mcp.json

# Python
__pycache__/
.venv/

# Trip data is tracked in git for backup
# (deploy.sh reads trips/ and pushes rendered HTML to gh-pages)

# Build artifacts (deploy.sh regenerates)
index.html
