.envrc
trek/
trek-src/
.claude/
.mcp.json

# Python
__pycache__/
.venv/

# Trip data (local only — deploy.sh reads local files, pushes HTML to gh-pages)
trips/

# Build artifacts (deploy.sh regenerates)
index.html
